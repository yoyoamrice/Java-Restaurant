name: Build and Push Docker Image

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # 1. Download your code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Set up Java 17 (Same as your local environment)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'microsoft' # or 'temurin' if you prefer

      # 3. Build and Test the application with Gradle
  - name: Build and Test with Gradle
    env:
      MYSQL_URL: ${{ secrets.MYSQL_URL }}
      MYSQL_USER: ${{ secrets.MYSQL_USER }}
      MYSQL_PASS: ${{ secrets.MYSQL_PASS }}
    run: ./gradlew clean build -x jar

      # 4. Set up Docker Buildx (required for modern Docker builds)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 5. Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 6. Build and Push the Docker image
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/restaurant:v${{ github.run_number }}
  # 7. Log in to Azure
  - name: Log in to Azure
    uses: azure/login@v2
    with:
      creds: ${{ secrets.AZURE_CREDENTIALS }}

  # 8. Tell Azure to pull the new image
  - name: Update Azure Container App
    run: |
      az config set extension.use_dynamic_install=yes_without_prompt
      az containerapp update \
        --name restaurant-app \
        --resource-group restaurant-rg \
        --image ${{ secrets.DOCKERHUB_USERNAME }}/restaurant:v${{ github.run_number }}
